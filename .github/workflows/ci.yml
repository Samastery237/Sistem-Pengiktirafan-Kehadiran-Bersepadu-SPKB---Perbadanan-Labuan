name: CI Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  DJANGO_DEBUG: 'False'
  DJANGO_ALLOWED_HOSTS: 'localhost,127.0.0.1'
  DOCKER_IMAGE: ${{ secrets.DOCKER_USERNAME || 'spkb' }}/spkb

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
        cache: 'pip'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libcairo2-dev pkg-config meson python3-dev
        python3 -c "
import sysconfig, os, subprocess
pc_dir = sysconfig.get_path('data') + '/lib/pkgconfig'
src = '/usr/lib/x86_64-linux-gnu/pkgconfig/cairo.pc'
if os.path.exists(src) and os.path.isdir(pc_dir):
    subprocess.run(['ln', '-sf', src, pc_dir + '/cairo.pc'], check=True)
    print('Linked cairo.pc into', pc_dir)
else:
    print('WARN: src exists:', os.path.exists(src), 'pc_dir exists:', os.path.isdir(pc_dir))
"

    - name: Install Python dependencies
      working-directory: ./backend
      env:
        PKG_CONFIG_PATH: /usr/lib/x86_64-linux-gnu/pkgconfig:/usr/share/pkgconfig
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Lint with Ruff
      working-directory: ./backend
      run: pip install ruff && ruff check .

  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
    - uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
        cache: 'pip'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libpango-1.0-0 libpangoft2-1.0-0 libcairo2 libcairo2-dev pkg-config meson python3-dev
        # pycairo's meson build overrides PKG_CONFIG_PATH with Python's own
        # pkgconfig dir, so we symlink cairo.pc into that directory
        python3 -c "
import sysconfig, os, subprocess
pc_dir = sysconfig.get_path('data') + '/lib/pkgconfig'
src = '/usr/lib/x86_64-linux-gnu/pkgconfig/cairo.pc'
if os.path.exists(src) and os.path.isdir(pc_dir):
    subprocess.run(['ln', '-sf', src, pc_dir + '/cairo.pc'], check=True)
    print('Linked cairo.pc into', pc_dir)
else:
    print('WARN: src exists:', os.path.exists(src), 'pc_dir exists:', os.path.isdir(pc_dir))
"

    - name: Install Python dependencies
      working-directory: ./backend
      env:
        PKG_CONFIG_PATH: /usr/lib/x86_64-linux-gnu/pkgconfig:/usr/share/pkgconfig
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Django Tests
      working-directory: ./backend
      env:
        DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY || 'ci-secret-key-for-github-actions-minimum-50-chars-long-ok' }}
      run: python manage.py test --parallel

  e2e-tests:
    name: E2E Tests
    runs-on: ubuntu-latest
    needs: lint
    steps:
    - uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.13'
        cache: 'pip'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libpango-1.0-0 libpangoft2-1.0-0 libcairo2 libcairo2-dev pkg-config meson python3-dev
        # pycairo's meson build overrides PKG_CONFIG_PATH with Python's own
        # pkgconfig dir, so we symlink cairo.pc into that directory
        python3 -c "
import sysconfig, os, subprocess
pc_dir = sysconfig.get_path('data') + '/lib/pkgconfig'
src = '/usr/lib/x86_64-linux-gnu/pkgconfig/cairo.pc'
if os.path.exists(src) and os.path.isdir(pc_dir):
    subprocess.run(['ln', '-sf', src, pc_dir + '/cairo.pc'], check=True)
    print('Linked cairo.pc into', pc_dir)
else:
    print('WARN: src exists:', os.path.exists(src), 'pc_dir exists:', os.path.isdir(pc_dir))
"

    - name: Install Python dependencies
      working-directory: ./backend
      env:
        PKG_CONFIG_PATH: /usr/lib/x86_64-linux-gnu/pkgconfig:/usr/share/pkgconfig
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Set up Node.js
      uses: actions/setup-node@v6
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install Node dependencies
      run: npm ci || npm install

    - name: Cache Playwright browsers
      id: cache-playwright
      uses: actions/cache@v6
      with:
        path: ~/.cache/ms-playwright
        key: playwright-${{ runner.os }}-${{ hashFiles('package-lock.json') }}

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps chromium

    - name: Run Django Migrations
      working-directory: ./backend
      env:
        DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY || 'ci-secret-key-for-github-actions-minimum-50-chars-long-ok' }}
      run: |
        python manage.py makemigrations
        python manage.py migrate
        python manage.py createcachetable || true

    - name: Set E2E test credentials
      working-directory: ./backend
      env:
        DJANGO_SECRET_KEY: ${{ secrets.DJANGO_SECRET_KEY || 'ci-secret-key-for-github-actions-minimum-50-chars-long-ok' }}
      run: |
        python manage.py createsuperuser --noinput --username admin --email admin@example.com --skip-checks || true
        python manage.py shell -c "from django.contrib.auth.models import User; u=User.objects.get(username='admin'); u.set_password('admin123'); u.save()"

    - name: Run E2E Tests
      run: npx playwright test

    - name: Upload Test Results
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30

  docker:
    name: Docker Build & Push
    runs-on: ubuntu-latest
    needs: [backend-tests, e2e-tests]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
    - uses: actions/checkout@v7

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push
      uses: docker/build-push-action@v6
      with:
        context: .
        push: true
        tags: ${{ env.DOCKER_IMAGE }}:latest,${{ env.DOCKER_IMAGE }}:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Scan image for CVEs
      uses: docker/scout-action@v1
      with:
        command: quickview,cves
        image: ${{ env.DOCKER_IMAGE }}:latest
